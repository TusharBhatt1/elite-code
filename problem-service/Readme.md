Model->Repository(code DB interaction)=>Service(Wrapper on Repository)=>Controller


- CLASSES ARE USED TO GROUP RELATED DATA AND BEHAVIORS AS A SINGLE REUSABLE UNIT
   - public/private are access specifiers


- We require call/apply/bind-> required not generally with methods but when want to invoke as a function
    - JavaScript determines this based on how a function is called, not where it was defined. 